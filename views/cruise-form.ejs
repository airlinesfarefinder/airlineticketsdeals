<div class="flight-form-container">
        <div class="form-content">
            <form method="GET" action="https://airlineticketsdeals.com/book" onsubmit="return first_form()">
                <!-- Trip Type Selection -->
                <div class="trip-type">
                    <div class="radio-container">
                        <input type="radio" id="roundtrip" name="tripType" value="roundtrip" checked onclick="show_date(this.value)">
                        <label for="roundtrip" class="radio-label">
                            <span class="radio-dot"></span>
                            Round Trip
                        </label>
                    </div>
                    <div class="radio-container">
                        <input type="radio" id="oneway" name="tripType" value="oneway" onclick="show_date(this.value)">
                        <label for="oneway" class="radio-label">
                            <span class="radio-dot"></span>
                            One Way
                        </label>
                    </div>
                </div>

                <!-- Form Fields Grid -->
                <div class="form-grid">
                    <!-- From Location -->
                    <div class="form-group">
                        <!-- <label class="form-label">From</label> -->
                        <label class="form-label" style="font-weight: bold; font-size: 1.2rem;color:rgb(46, 43, 43);">From</label>

                        <div class="input-container">
                            <!-- <i class="fas fa-plane-departure input-icon"></i> -->

                            <i class="fas fa-ship input-icon"></i>

                            <input type="text" 
                                   class="form-input" 
                                   name="origin1" 
                                   id="location"
                                   placeholder="Cruise Port"
                                   required
                                   autocomplete="off"
                                   onclick="clearInput2()">
                        </div>
                    </div>

                    <!-- To Location -->
                    <div class="form-group">
                        <!-- <label class="form-label">To</label> -->

                       <label class="form-label" style="font-weight: bold; font-size: 1.2rem;color:rgb(46, 43, 43);">To</label>

                        <div class="input-container">
                            <!-- <i class="fas fa-plane-arrival input-icon"></i> -->

                            <i class="fas fa-ship input-icon"></i>

                            <input type="text" 
                                   class="form-input" 
                                   name="destination1" 
                                   id="location2"
                                   placeholder="Cruise Port"
                                   required
                                   autocomplete="off"
                                   onclick="clearInput()">
                        </div>
                    </div>

                    <!-- Date Fields Row -->
                    <div class="form-row">
                        <!-- Departure Date -->
                        <div class="form-group">
                              <label class="form-label" style="font-weight: bold; font-size: 1.2rem;color:rgb(46, 43, 43);">Departure Date</label>
                            <!-- <label class="form-label">Departure Date</label> -->
                            <div class="input-container">
                                <i class="fas fa-calendar-alt input-icon"></i>
                                <input type="hidden" id="depDt" name="departDate1" value="">
                                <input type="text" 
                                       class="form-input" 
                                       id="datepicker"
                                       name="departDate1"
                                       placeholder="Select"
                                       required
                                       readonly
                                       autocomplete="off">
                            </div>
                        </div>

                        <!-- Return Date -->
                        <div class="form-group">
                               <label class="form-label" style="font-weight: bold; font-size: 1.2rem;color:rgb(46, 43, 43);">Return Date</label>
                            <!-- <label class="form-label">Return Date</label> -->
                            <div class="input-container">
                                <i class="fas fa-calendar-alt input-icon"></i>
                                <input type="hidden" id="retDt" name="returnDate1" value="">
                                <input type="text" 
                                       class="form-input" 
                                       id="datepicker2"
                                       name="returnDate1"
                                       placeholder="Select"
                                       required
                                       readonly
                                       autocomplete="off">
                            </div>
                        </div>
                    </div>

<!-- Travelers & Class -->
<div class="form-group travelers-section">

     <label class="form-label" style="font-weight: bold; font-size: 1.2rem;color:rgb(46, 43, 43);">Travellers & Class</label>
  <!-- <label class="form-label">Travellers & Class</label> -->
  <div class="input-container">
    <i class="fas fa-user-friends input-icon"></i>
    <input type="text" 
           class="form-input travelers-input" 
           id="btm_clk"
           placeholder="1 Passenger, Economy"
           readonly 
           onclick="document.getElementById('travelers-dropdown').style.display='block';"
           style="cursor:pointer;">

    <div class="travelers-dropdown" 
         id="travelers-dropdown" 
         style="display: none; border: 1px solid #ccc; padding: 15px; background: #fff; position: absolute; z-index: 999; width: 280px;">
      
      <!-- Class Selection -->
      <select class="class-select" id="classType" name="classType" style="margin-bottom: 15px; width: 100%; padding: 5px;">
        <option value="Economy" selected>Economy</option>
        <option value="Premium Economy">Premium Economy</option>
        <option value="Business Class">Business Class</option>
        <option value="First Class">First Class</option>
      </select>

      <!-- Adults -->
      <div class="form-group" style="margin-bottom: 15px; justify-items: center;">
        <div>
          <h5 style="margin: 0;">Adults</h5>
          <span style="font-size: 12px; color: gray;">17+ years</span>
        </div>
        <div class="input-group passengers-group" style="margin-top: 5px;">
          <button type="button" style="width: 25px; height: 25px;" onclick="changeCount('adults', -1)">−</button>
          <input type="text" id="adults" name="adults" value="1" readonly style="width: 40px; text-align: center; margin: 0 12px;">
          <button type="button" style="width: 25px; height: 25px;" onclick="changeCount('adults', 1)">+</button>
        </div>
      </div>

      <!-- Children -->
      <div class="form-group" style="margin-bottom: 15px; justify-items: center;">
        <div>
          <h5 style="margin: 0;">Children</h5>
          <span style="font-size: 12px; color: gray;">0–17 years</span>
        </div>
        <div class="input-group passengers-group" style="margin-top: 5px;">
          <button type="button" style="width: 25px; height: 25px;" onclick="changeCount('children', -1)">−</button>
          <input type="text" id="children" name="children" value="0" readonly style="width: 40px; text-align: center; margin: 0 12px;">
          <button type="button" style="width: 25px; height: 25px;" onclick="changeCount('children', 1)">+</button>
        </div>
      </div>

      <!-- Infants -->
      <div class="form-group" style="margin-bottom: 15px;  justify-items: center;">
        <div>
          <h5 style="margin: 0;">Infants</h5>
          <span style="font-size: 12px; color: gray;">Up to 2 years</span>
        </div>
        <div class="input-group passengers-group" style="margin-top: 5px;">
          <button type="button" style="width: 25px; height: 25px;" onclick="changeCount('infants', -1)">−</button>
          <input type="text" id="infants" name="infants" value="0" readonly style="width: 40px; text-align: center; margin: 0 12px;">
          <button type="button" style="width: 25px; height: 25px;" onclick="changeCount('infants', 1)">+</button>
        </div>
      </div>

      <!-- Done Button -->
      <button type="button" 
              id="travellersDoneBtn" 
              class="done-btn" 
              onclick="updateTravellerSummary()"
              style="margin-top: 10px; padding: 6px 12px; background: #007bff; color: white; border: none; cursor: pointer; border-radius: 4px;">
        Done
      </button>
    </div>
  </div>
</div>

<!-- JS SCRIPT -->
<script>
  function changeCount(type, delta) {
    const input = document.getElementById(type);
    let value = parseInt(input.value);
    value = isNaN(value) ? 0 : value + delta;
    if (value < 0) value = 0;
    if (type === 'adults' && value === 0) value = 1; // Always have at least 1 adult
    input.value = value;
  }

  function updateTravellerSummary() {
    const adults = parseInt(document.getElementById("adults").value) || 0;
    const children = parseInt(document.getElementById("children").value) || 0;
    const infants = parseInt(document.getElementById("infants").value) || 0;
    const total = adults + children + infants;

    const classType = document.getElementById("classType").value;
    const passengerText = `${total} Passenger${total > 1 ? 's' : ''}, ${classType}`;
    document.getElementById("btm_clk").value = passengerText;

    document.getElementById("travelers-dropdown").style.display = "none";
  }
</script>





                </div>

                <!-- Search Button -->
              <button type="submit" class="btn btn-primary btn-block">
  <i class="fa fa-plane-departure"></i> Search Cruise</button>
            </form>
        </div>
    </div>



    <script>




  // Increment/Decrement logic
  function setupIncDec(idMinus, idInput, idPlus, min, max) {
    document.getElementById(idMinus).onclick = function() {
      let input = document.getElementById(idInput);
      let val = parseInt(input.value, 10);
      if (val > min) input.value = val - 1;
    };
    document.getElementById(idPlus).onclick = function() {
      let input = document.getElementById(idInput);
      let val = parseInt(input.value, 10);
      if (val < max) input.value = val + 1;
    };
  }
  setupIncDec('adult-minus', 'adults', 'adult-plus', 1, 9);
  setupIncDec('child-minus', 'children', 'child-plus', 0, 9);
  setupIncDec('infant-minus', 'infants', 'infant-plus', 0, 9);
</script>

    <script>
        // Trip type functionality
        function show_date(tripType) {
            const returnDateField = document.getElementById('datepicker2');
            if (tripType === 'oneway') {
                returnDateField.disabled = true;
                returnDateField.value = '';
                returnDateField.removeAttribute('required');
                // Disable the return date picker
                if (window.returnDatePicker) {
                    window.returnDatePicker.destroy();
                }
            } else {
                returnDateField.disabled = false;
                returnDateField.setAttribute('required', 'required');
                // Reinitialize the return date picker
                if (!window.returnDatePicker || !window.returnDatePicker.isOpen) {
                    initializeReturnDatePicker();
                }
            }
        }

        // Travelers dropdown functionality
        document.getElementById('btm_clk').addEventListener('click', function() {
            const dropdown = document.getElementById('travelers-dropdown');
            dropdown.classList.toggle('active');

        });

        // Close dropdown when clicking outside
        document.addEventListener('click', function(event) {
            const dropdown = document.getElementById('travelers-dropdown');
            const input = document.getElementById('btm_clk');
            if (!dropdown.contains(event.target) && event.target !== input) {
                dropdown.classList.remove('active');

            }

            
        });

        // Counter functions - Fixed to prevent event bubbling
        function increase_adult_rt() {
            event.preventDefault();
            event.stopPropagation();
            const counter = document.getElementById('AdultsRT');
            const hidden = document.getElementById('adults-hidden');
            let value = parseInt(counter.textContent);
            if (value < 9) {
                value++;
                counter.textContent = value;
                hidden.value = value;
                updateTravelersDisplay();
            }
        }

        function decrease_adult_rt() {
            event.preventDefault();
            event.stopPropagation();
            const counter = document.getElementById('AdultsRT');
            const hidden = document.getElementById('adults-hidden');
            let value = parseInt(counter.textContent);
            if (value > 1) {
                value--;
                counter.textContent = value;
                hidden.value = value;
                updateTravelersDisplay();
            }
        }

        function increase_child_rt() {
            event.preventDefault();
            event.stopPropagation();
            const counter = document.getElementById('ChildrenRT');
            const hidden = document.getElementById('children-hidden');
            let value = parseInt(counter.textContent);
            if (value < 9) {
                value++;
                counter.textContent = value;
                hidden.value = value;
                updateTravelersDisplay();
            }
        }

        function decrease_child_rt() {
            event.preventDefault();
            event.stopPropagation();
            const counter = document.getElementById('ChildrenRT');
            const hidden = document.getElementById('children-hidden');
            let value = parseInt(counter.textContent);
            if (value > 0) {
                value--;
                counter.textContent = value;
                hidden.value = value;
                updateTravelersDisplay();
            }
        }

        function increase_infant_rt() {
            event.preventDefault();
            event.stopPropagation();
            const counter = document.getElementById('InfantsRT');
            const hidden = document.getElementById('infants-hidden');
            let value = parseInt(counter.textContent);
            if (value < 9) {
                value++;
                counter.textContent = value;
                hidden.value = value;
                updateTravelersDisplay();
            }
        }

        function decrease_infant_rt() {
            event.preventDefault();
            event.stopPropagation();
            const counter = document.getElementById('InfantsRT');
            const hidden = document.getElementById('infants-hidden');
            let value = parseInt(counter.textContent);
            if (value > 0) {
                value--;
                counter.textContent = value;
                hidden.value = value;
                updateTravelersDisplay();
            }
        }

        function updateTravelersDisplay() {
            const adults = parseInt(document.getElementById('AdultsRT').textContent);
            const children = parseInt(document.getElementById('ChildrenRT').textContent);
            const infants = parseInt(document.getElementById('InfantsRT').textContent);
            const classType = document.getElementById('classType').options[document.getElementById('classType').selectedIndex].text;
            
            const total = adults + children + infants;
            const passengerText = total === 1 ? 'Passenger' : 'Passengers';
            
            document.getElementById('btm_clk').value = `${total} ${passengerText}, ${classType}`;
        }

        function all_pesenger() {


            //   const input = document.getElementById('btm_clk');
            // if (!dropdown.contains(event.target) && event.target !== input) {
            //     dropdown.classList.remove('active');

            // }
          
            //   document.getElementById('travellersDropdown').style.display = 'none';

            // document.getElementById('travelers-dropdown').classList.remove('active');

            //     document.querySelector('.travellers-dropdown').classList.remove('show');

                  
 
            // document.getElementById('travelers-dropdown').style.display = 'none';

               document.getElementById('btm_clk').style.display = 'none';
//  updateTravelersDisplay();
                // document.querySelector('travellersDropdown').classList.remove('show');

            

            
    }

        // Class change handler
        document.getElementById('classType').addEventListener('change', updateTravelersDisplay);

        // Placeholder functions for form validation and input clearing
        function first_form() {
            // Add your form validation logic here
            return true;
        }

        function clearInput() {
            // Add your input clearing logic here
        }

        function clearInput2() {
            // Add your input clearing logic here
        }

        // Get current month range
        function getCurrentMonthRange() {
            const today = new Date();
            const firstDay = new Date(today.getFullYear(), today.getMonth(), 1);
            const lastDay = new Date(today.getFullYear(), today.getMonth() + 1, 0);
            return { firstDay, lastDay };
        }

        // Initialize return date picker separately
        function initializeReturnDatePicker() {
            const { firstDay, lastDay } = getCurrentMonthRange();
            window.returnDatePicker = flatpickr("#datepicker2", {
                dateFormat: "Y-m-d",
                minDate: document.getElementById('datepicker').value || firstDay,
                maxDate: lastDay,
                onChange: function(selectedDates, dateStr, instance) {
                    document.getElementById('retDt').value = dateStr;
                }
            });
        }

        // Initialize date pickers with Flatpickr - Limited to current month only
        // function initializeDatePickers() {
        //     const { firstDay, lastDay } = getCurrentMonthRange();
            
        //     const departureDate = flatpickr("#datepicker", {
        //         dateFormat: "Y-m-d",
        //         minDate: firstDay,
        //         maxDate: lastDay,
        //         onChange: function(selectedDates, dateStr, instance) {
        //             document.getElementById('depDt').value = dateStr;
        //             // Set minimum return date to departure date
        //             if (window.returnDatePicker) {
        //                 window.returnDatePicker.set('minDate', dateStr);
        //             }
        //         }
        //     });

        //     initializeReturnDatePicker();

        //     // Store references for later use
        //     window.departureDatePicker = departureDate;
        // }



        function initializeDatePickers() {
    const { firstDay } = getCurrentMonthRange();

    const departureDate = flatpickr("#datepicker", {
        dateFormat: "Y-m-d",
        minDate: firstDay,
        showMonths: 1, // Ensure only one month is shown
        onChange: function(selectedDates, dateStr, instance) {
            document.getElementById('depDt').value = dateStr;
            // Set minimum return date to departure date
            if (window.returnDatePicker) {
                window.returnDatePicker.set('minDate', dateStr);
            }
        }
    });

    initializeReturnDatePicker();

    // Store references for later use
    window.departureDatePicker = departureDate;
}

        // Initialize date pickers when page loads
        document.addEventListener('DOMContentLoaded', function() {
            initializeDatePickers();
        });
    </script>




 <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            min-height: 100vh;
            padding: 20px 10px;
        }




/* making form more compact  */

/* Add to your form1.ejs or main CSS */
.flight-form-container {
  padding: 18px 20px;
  background: #fff;
  border-radius: 12px;
  box-shadow: 0 2px 12px rgba(0,0,0,0.08);
  max-width: 420px;
  margin: 0 auto;
}
.form-group {
  margin-bottom: 12px;
}
.input-group {
  display: flex;
  align-items: center;
}
label {
  font-size: 1rem;
  margin-bottom: 4px;
}
.btn-primary.btn-block {
  width: 100%;
  font-size: 1.2rem;
  padding: 10px 0;
  margin-top: 10px;
}


 /* button - and + */

 /* Add to your form1.ejs or main CSS */
.btn-incdec {
  font-size: 2rem;
  width: 48px;
  height: 48px;
  border-radius: 50%;
  margin: 0 8px;
  background: #106e00;
  color: #fff;
  border: none;
  transition: background 0.2s;
}
.btn-incdec:hover {
  background: #0a3788;
}
.passengers-group input[type="text"] {
  width: 48px;
  text-align: center;
  font-size: 1.5rem;
  border: 1px solid #ccc;
  border-radius: 6px;
}






        .flight-form-container {
            max-width: 1200px;
            margin: 0 auto;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            backdrop-filter: blur(10px);
            border-radius: 20px;
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1);
            overflow: hidden;
        }

        .form-content {
            padding: 30px 20px;
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
        }

        .trip-type {
            display: flex;
            gap: 20px;
            margin-bottom: 30px;
            justify-content: center;
        }

        .radio-container {
            position: relative;
        }

        .radio-container input[type="radio"] {
            display: none;
        }

        .radio-label {
            display: flex;
            align-items: center;
            gap: 10px;
            padding: 12px 24px;
            background: #f8fafc;
            border: 2px solid #e2e8f0;
            border-radius: 50px;
            cursor: pointer;
            transition: all 0.3s ease;
            font-weight: 500;
            color: #64748b;
        }

        .radio-container input[type="radio"]:checked + .radio-label {
            background: linear-gradient(135deg, #4f46e5 0%, #7c3aed 100%);
            color: white;
            border-color: #4f46e5;
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(79, 70, 229, 0.3);
        }

        .radio-dot {
            width: 16px;
            height: 16px;
            border: 2px solid currentColor;
            border-radius: 50%;
            position: relative;
        }

        .radio-container input[type="radio"]:checked + .radio-label .radio-dot::after {
            content: '';
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            width: 8px;
            height: 8px;
            background: white;
            border-radius: 50%;
        }

        .form-grid {
            display: grid;
            grid-template-columns: 1fr;
            gap: 20px;
            margin-bottom: 30px;
        }

        .form-row {
            display: grid;
            grid-template-columns: 1fr;
            gap: 20px;
        }

        .form-group {
            position: relative;
        }

        .form-label {
            display: block;
            margin-bottom: 8px;
            font-weight: 600;
            color: #374151;
            font-size: 14px;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .input-container {
            position: relative;
        }

        .input-icon {
            position: absolute;
            left: 16px;
            top: 50%;
            transform: translateY(-50%);
            color: #6b7280;
            font-size: 18px;
            z-index: 2;
        }

        .form-input {
            width: 100%;
            padding: 16px 16px 16px 50px;
            border: 2px solid #e5e7eb;
            border-radius: 12px;
            font-size: 16px;
            transition: all 0.3s ease;
            background: white;
            color: #374151;
        }

        .form-input:focus {
            outline: none;
            border-color: #4f46e5;
            box-shadow: 0 0 0 3px rgba(79, 70, 229, 0.1);
            transform: translateY(-1px);
        }

        .form-input:disabled {
            background: #f3f4f6;
            color: #9ca3af;
            cursor: not-allowed;
            opacity: 0.6;
        }

        .travelers-input {
            cursor: pointer;
        }

        .travelers-dropdown {
            position: absolute;
            top: 100%;
            left: 0;
            right: 0;
            background: white;
            border: 2px solid #e5e7eb;
            border-radius: 12px;
            box-shadow: 0 10px 40px rgba(0, 0, 0, 0.1);
            z-index: 9999;
            padding: 20px;
            margin-top: 8px;
            display: none;
        }

        .travelers-dropdown.active {
            display: block;
            animation: slideDown 0.3s ease;
        }

        @keyframes slideDown {
            from {
                opacity: 0;
                transform: translateY(-10px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .passenger-row {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 15px 0;
            border-bottom: 1px solid #f1f5f9;
        }

        .passenger-row:last-child {
            border-bottom: none;
        }

        .passenger-info h5 {
            font-weight: 600;
            color: #374151;
            margin-bottom: 4px;
        }

        .passenger-info span {
            font-size: 12px;
            color: #6b7280;
        }

        .counter-controls {
            display: flex;
            align-items: center;
            gap: 15px;
        }

        .counter-btn {
            width: 36px;
            height: 36px;
            border: 2px solid #e5e7eb;
            background: white;
            border-radius: 8px;
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            transition: all 0.2s ease;
            color: #6b7280;
        }

        .counter-btn:hover {
            border-color: #4f46e5;
            color: #4f46e5;
            transform: scale(1.05);
        }

        .counter-btn:disabled {
            opacity: 0.5;
            cursor: not-allowed;
            transform: none;
        }

        .counter-value {
            font-weight: 600;
            color: #374151;
            min-width: 30px;
            text-align: center;
        }

        .class-select {
            width: 100%;
            padding: 12px;
            border: 2px solid #e5e7eb;
            border-radius: 8px;
            background: white;
            font-size: 14px;
            margin-top: 15px;
        }

        .done-btn {
            width: 100%;
            padding: 12px;
            background: linear-gradient(135deg, #4f46e5 0%, #7c3aed 100%);
            color: white;
            border: none;
            border-radius: 8px;
            font-weight: 600;
            cursor: pointer;
            margin-top: 15px;
            transition: all 0.3s ease;
        }

        .done-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(79, 70, 229, 0.3);
        }

        .search-btn {
            width: 100%;
            padding: 18px;
            background: linear-gradient(135deg, #10b981 0%, #059669 100%);
            color: white;
            border: none;
            border-radius: 12px;
            font-size: 18px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 10px;
        }

        .search-btn:hover {
            transform: translateY(-3px);
            box-shadow: 0 15px 35px rgba(16, 185, 129, 0.4);
        }

        .search-btn:active {
            transform: translateY(-1px);
        }

        /* Mobile First Responsive Design */
        @media (min-width: 640px) {
            .form-content {
                padding: 40px 30px;
            }
            
            .form-row {
                grid-template-columns: 1fr 1fr;
            }
        }

        @media (min-width: 768px) {
            .form-content {
                padding: 50px 40px;
            }
            
            .form-grid {
                grid-template-columns: 1fr 1fr;
                gap: 30px;
            }
            
            .search-btn {
                max-width: 300px;
                margin: 0 auto;
            }
        }

        @media (min-width: 1024px) {
            .form-grid {
                grid-template-columns: 1fr 1fr 1fr;
            }
            
            .travelers-section {
                grid-column: span 1;
            }
        }

        @media (min-width: 1200px) {
            .form-content {
                padding: 60px 50px;
            }
        }

        /* Enhanced animations */
        .form-input, .counter-btn, .radio-label {
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        }

        .form-group:hover .form-input {
            border-color: #6366f1;
        }

        /* Accessibility improvements */
        @media (prefers-reduced-motion: reduce) {
            * {
                animation-duration: 0.01ms !important;
                animation-iteration-count: 1 !important;
                transition-duration: 0.01ms !important;
            }
        }
    </style>
